# Simple Prom UA Parser
Parse goods to CSV file (test.csv)

## Using

1. Create virtual environment

```sh
python3 -m venv .venv
```

2. Install dependencies

```sh
python -m pip install -r requirements.txt
```

3. Start parse

```sh
python prom/parser.py
```
